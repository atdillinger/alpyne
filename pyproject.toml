[project]
name = "alpyne"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pygments>=2.20.0",
]

[tool.uv.workspace]
members = [
    "lambdas/alpyne",
    "infra",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
